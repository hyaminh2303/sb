%a{href: 'javascript: void(0);'}= User::USER_BIDOPTIMIZATION
resumed the campaign (delivery realized:
= activity.parameters[:delivery_realized].to_f.round(2)
, delivery expected:
= activity.parameters[:delivery_expected].to_f.round(2)
)
